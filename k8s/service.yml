apiVersion: v1
kind: Service
metadata:
  name: netflix-app-service
  labels:
    app: netflix-app
spec:
  type: NodePort
  selector:
    app: netflix-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
